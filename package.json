{
  "name": "wp-precommit-hooks",
  "version": "1.1.0",
  "description": "WordPress pre-commit hooks package",
  "private": true,
  "license": "ISC",
  "scripts": {
    "build": "echo \"No frontend build required for wp-precommit-hooks\"",
    "lint": "echo \"Lint runs in host project hooks\""
  },
  "engines": {
    "node": ">=20"
  }
}
